py.extension_module(
  'raster',
  ['raster.cpp', 'raster.h'],
  dependencies: [lib_deps],
  include_directories: inc_dir,
  install: true,
)

py.extension_module(
  'vector',
  ['vector.cpp', 'vector.h'],
  dependencies: [lib_deps],
  include_directories: inc_dir,
  install: true,
)
