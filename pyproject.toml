[build-system]
#TODO build backend may change
build-backend = "mesonpy"
requires = [
#	"meson-python>=VERSION" TODO add version
#	"GDAL>=VERSION" TODO add version
]

[project]
name = "sgs"
version = "0.0.0"

#TODO add license
#license = {file = "LICENSE.txt"}

[tool.pytest.ini_options]
pythonpath = "."
