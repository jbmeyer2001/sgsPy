[build-system]
build-backend = "mesonpy"
requires = [
	"meson-python"
]

[project]
name = "sgs"
version = "0.0.0"
dependencies = [
#	"gdal >= VERSION" TODO add version
#	"numpy >= VERSION" TODO add version
#	"matplotlib >= VERSION" TODO add version
]

#TODO add license
#license = {file = "LICENSE.txt"}

[tool.meson-python.args]
setup = ['-Db_vscrt=mt']

[tool.pytest.ini_options]
pythonpath = "."
testpaths = [
	"tests"
]
